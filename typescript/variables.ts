let a: boolean = true;
let b: number = 2;
let c: string = "2";

// Khai báo mảng
let d: Array<number> = [2, 1];
let list: number[] = [0, 1, 2];

// Khai bảo tuple
let arr: [string, number] = ["Hello", 2]

// Khai báo enum
enum Color { Red = 1, Blue = 2, Green = 3 };
let color: Color = Color.Red
